#include<stdio.h>
int main()
{
    
    short int A=10;//its is used to reduce the size of variable
    
    long int B=35;//its shows current size 
        printf("%d\n%d\n",sizeof(A),sizeof(B));
        return 0;
}